# Optional in production, for example https://bff.example.com
# Leave empty locally so Vite proxies /api to FastAPI.
VITE_API_BASE_URL=

# Backend-only settings. Keep false for polished local mock responses.
USE_REAL_OPENAI=false
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
